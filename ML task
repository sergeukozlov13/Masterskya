import pandas as pd
import numpy as np
from sklearn.model_selection import train_test_split
from sklearn.preprocessing import StandardScaler

Задание: 
Вернёмся к Facebook. Задача — спрогнозировать суммарную активность на официальных аккаунтах компаний в зависимости от параметров этих страниц. В следующих уроках вы обучите линейную модель. А сейчас:
выведите список признаков датасета;
разделите выборку на обучающую и валидационную методом train_test_split();
посчитайте среднее и стандартное отклонения для признака 'Page total likes' в train-выборке;
стандартизируйте эти выборки;
выведите среднее и стандартное отклонения признака 'Page total likes' для train- и test-выборок. Что можно заметить? Как это объяснить?# прочитайте данные с атрибутами аккаунтов компаний на Facebook и активностью на них

  
  
  Решение:
fb = pd.read_csv('/datasets/dataset_facebook_cosmetics.csv', sep = ';')  
# разделяем данные на признаки (матрица X) и целевую переменную (y)
X = fb.drop('Total Interactions', axis = 1)
y = fb['Total Interactions']

# выведите название признаков в датасете
print(X.columns) # ваш код здесь

# разделяем модель на обучающую и валидационную выборку
X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2)

# выведите среднее и стандартное отклонение признака 'Page total likes'
print('Mean for train', X_train['Page total likes'].mean())
print('Std for train',  X_train['Page total likes'].std())

# стандартизируем данные 
scaler = StandardScaler()
scaler.fit(X_train) # обучите scaler на обучающей выборке методом fit
X_train_st =scaler.transform(X_train) # стандартизируйте обучающую выборку методом transform scaler 
X_test_st = scaler.transform(X_test) # тандартизируйте тестовую выборку методом transform scaler

print('Mean for standardized train', np.mean(X_train_st[:,0]))
print('Std for standardized train', np.std(X_train_st[:,0]))
print('Mean for standardized test',  np.mean(X_test_st[:,0]))
print('Std for standardized test',  np.std(X_test_st[:,0]))

Выдает ошибку: слишком мало позиционных аргументов в строке 20 
